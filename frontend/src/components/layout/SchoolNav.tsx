'use client';

/**
 * SchoolNav is now integrated into the unified SchoolLayoutShell & SchoolSidebar.
 * Returns null to maintain compatibility with existing page imports without rendering duplicate headers.
 */
export function SchoolNav() {
  return null;
}
